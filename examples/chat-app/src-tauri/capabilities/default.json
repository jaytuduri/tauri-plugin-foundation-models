{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the chat app",
  "platforms": ["macOS"],
  "windows": ["main"],
  "permissions": [
    "core:default",
    "apple-intelligence:default"
  ]
}
